// Place your settings in this file to overwrite default and user settings.
{
    "editor.fontSize": 18,
    "editor.tabSize": 2,
    "files.exclude": {
    "**/*.js": { "when": "$(basename).ts"},
    "**/*?.js": { "when": "$(basename).tsx"}
  }
}